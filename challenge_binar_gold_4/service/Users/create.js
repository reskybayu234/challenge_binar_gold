const { User } = require('../../models');

// User.create({
//     user_name : req.body.name,
//     user_email : req.body.email,
//     user_password : req.body.password
// }).then(user => console.log("berhasil dibuat"))
// .catch(error => {console.log(error)})